import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hola mundo_2006251',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
