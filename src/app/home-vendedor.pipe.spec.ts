import { HomeVendedorPipe } from './home-vendedor.pipe';

describe('HomeVendedorPipe', () => {
  it('create an instance', () => {
    const pipe = new HomeVendedorPipe();
    expect(pipe).toBeTruthy();
  });
});
