import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-clase-2086021',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
