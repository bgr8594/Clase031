import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-clase-2003216',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
