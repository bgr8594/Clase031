import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-clase-1844640',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
