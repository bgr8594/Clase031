import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'primer avance de PIA',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
