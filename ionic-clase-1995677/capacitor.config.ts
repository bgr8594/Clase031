import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-clase-1995677',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
